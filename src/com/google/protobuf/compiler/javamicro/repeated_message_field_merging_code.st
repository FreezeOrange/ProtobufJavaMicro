if (!other.<variables.name>_.isEmpty()) {
    if (result.<variables.name>_.isEmpty()) {
        result.<variables.name>_ = new java.util.ArrayList\<<variables.type>\>();
    }
    result.<variables.name>_.addAll(other.<variables.name>_);
}